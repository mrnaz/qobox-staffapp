module.exports = {
  dependencies: {
    'react-native-worklets': {
      platforms: {
        android: null, // disable Android platform, so it won't be autolinked
        ios: null, // disable iOS platform, so it won't be autolinked
      },
    },
  },
};

